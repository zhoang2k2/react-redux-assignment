function TableHead() {
  return (
    <>
      <thead>
        <tr>
          {/* {TheadItem.map((item) => {
            return <th key={item.id}>{item.name}</th>;
          })} */}
        </tr>
      </thead>
    </>
  );
}

export default TableHead;
